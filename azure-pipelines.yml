trigger:
- master

pool:
  name: Default  # Name of your self-hosted agent pool

steps:
- script: echo "Build passed!"
  displayName: "✅ Simple build step"

steps:
- script: |
    echo "Simulating build..."
    sleep 2
    echo "✔️ Done!"
  displayName: "Simulated build"
